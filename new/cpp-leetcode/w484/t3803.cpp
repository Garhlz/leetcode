#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int residuePrefixes(string s) {
        vector<int> nums(26,0);
        for(int i = 0;i < s.size();i++){
            int c = s[i] - 'a';
            nums
        }
    }
};
